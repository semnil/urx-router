# セキュリティポリシー

> For the English version, see [SECURITY.md](SECURITY.md).

URX Router はデスクトップアプリケーションとブラウザデモで構成される。サーバーコンポーネント・
アカウント機構・テレメトリはいずれも持たない。デスクトップ版が自発的に行う通信は GitHub リリース
に対する更新確認のみで、環境設定と最近開いた計画の一覧は WebView の `localStorage` に保存され、
計画はユーザーが選んだファイルとして扱われる。

## サポート対象バージョン

サポート対象は最新リリースのみ。デスクトップ版は Tauri の updater で自動更新され、修正は
バックポートではなく新しいリリースとして提供する。

## 脆弱性の報告

GitHub の非公開報告を使うこと —
**[Security → Report a vulnerability](https://github.com/semnil/urx-router/security/advisories/new)**。
セキュリティ上の問題を公開 issue に投稿しないこと。

記載してほしい情報:

- 対象のビルド（デスクトップ Windows / デスクトップ macOS / ブラウザデモ）とバージョン
  （ツールバーの歯車から開く環境設定モーダルの末尾に表示）
- 攻撃者が何を得られるか、および再現手順
- 再現に必要な計画 JSON・`?plan=` リンク・ファイル — 実機の値を本文に貼らず、ファイルとして添付する

単独メンテナーのプロジェクトのため、対応はベストエフォート。初回返信の目安は 1 週間程度。
希望がない限り、アドバイザリに報告者としてクレジットする。

## 対象に含まれるもの

- 細工された計画 JSON・`?plan=` ディープリンク・ドロップされたファイル・設定ファイルの取り込みから
  到達できるコード実行、またはユーザーが選択していないパスへの読み書き
- Web コンテンツからの Tauri IPC 境界の悪用
- アプリが DOM として描画する内容を経由したインジェクション — サードパーティライセンスモーダル、
  計画の読み込み失敗レポート、実機から読み出した名前
- このプロジェクトの署名済みビルド以外の成果物を更新経路が受け入れてしまう問題
- リポジトリ・CI・リリース成果物に含まれる秘密情報や署名素材の露出
- ブラウザデモ <https://urx-router.semnil.com>

## 対象に含まれないもの

- **実機への破壊的な書き込み。** 計画の書き込みは接続中の本体の現在のミキサー設定を上書きし、
  ライブ同期は編集のたびに実機へ反映する。これは本ツールの目的であって不具合ではない —
  [免責事項](README.ja.md#免責事項)、初回起動時の同意ゲート、インストーラーが表示するライセンスを参照。
- 独自解析で判明した制御プロトコルによる実機操作、そのこと自体
- `--experimental` 起動フラグでのみ有効になる診断機能
- 実機ファームウェア・本体そのもの・ヤマハ製ソフトウェアの脆弱性 — ヤマハに報告すること
- 攻撃者が既にユーザー権限でコードを実行できることを前提とする指摘。本アプリはローカルマシンを
  信頼する（`localStorage`、ユーザーが開く計画ファイル、ユーザーが選択する MIDI ポート）
- 意図的に巨大な計画ファイルによるリソース枯渇
- 影響を実証していない自動スキャナーの出力
