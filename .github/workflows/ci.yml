name: CI

# Lightweight checks on every PR and on pushes to main. Heavier jobs
# (Playwright E2E, third-party license generation) run post-merge; see
# post-merge.yml.
# Doc-only changes (Markdown, docs/) don't affect the build or tests, so skip
# these checks for them — except the skill's generated model references, which
# skill-export.test.ts diffs against the generator, so an .md-only edit there
# must still run the unit tests. Patterns are ordered: include everything,
# negate docs, then re-include the generated files (the last match wins).
on:
  pull_request:
    paths:
      - '**'
      - '!**/*.md'
      - '!docs/**'
      - '.claude/skills/urx-routing-planner/references/model-*.md'
  push:
    branches: [main]
    paths:
      - '**'
      - '!**/*.md'
      - '!docs/**'
      - '.claude/skills/urx-routing-planner/references/model-*.md'

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      # Three handles are build-gated. Two are dev-only (import.meta.env.DEV): the meter
      # bench's console handle (src/ui/console.ts) and the keyboard measurement harness
      # (src/ui/keyprobe.ts, installed from src/main.ts). The third, the race harness's
      # plan-ledger probe (src/ui/trace-probe.ts), rides its own build flag (VITE_TRACE,
      # .env.trace) so the instrumented E2E tier can be served a production-SHAPED bundle
      # rather than a dev one. A plain build drops all three statically; this is the guard
      # that they stay dropped. Emitted code only — the source map carries the original
      # text of the dropped branch by design. The other half of each contract — that the
      # handle still carries what its driver uses — is pinned in
      # src/ui/meter-bench.contract.test.ts and src/ui/keyprobe.test.ts.
      - name: Dev-only hooks stay out of the bundle
        run: |
          ! grep -rl --include='*.js' --include='*.html' "__urxConsole" dist
          ! grep -rl --include='*.js' --include='*.html' "__urxKeyProbe" dist
          ! grep -rl --include='*.js' --include='*.html' "__urxTrace" dist
      - run: pnpm typecheck:e2e
      - run: pnpm test
