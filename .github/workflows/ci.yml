name: CI

# Lightweight checks on every PR and on pushes to main: the build, the unit tests and
# the ordinary E2E suite. Third-party license generation runs post-merge (post-merge.yml)
# and the live-sync race harness runs from race.yml, which nothing triggers per-merge.
# Doc-only changes (Markdown, docs/) don't affect the build or tests, so skip
# these checks for them — except the skill's generated model references, which
# skill-export.test.ts diffs against the generator, so an .md-only edit there
# must still run the unit tests. Patterns are ordered: include everything,
# negate docs, then re-include the generated files (the last match wins).
on:
  pull_request:
    paths:
      - '**'
      - '!**/*.md'
      - '!docs/**'
      - '.claude/skills/urx-routing-planner/references/model-*.md'
  push:
    branches: [main]
    paths:
      - '**'
      - '!**/*.md'
      - '!docs/**'
      - '.claude/skills/urx-routing-planner/references/model-*.md'

jobs:
  frontend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
        timeout-minutes: 10 # hang detector, not a budget — see .github/actions/install-browsers
      # The code-side half of the "## Reusable assets" index: a renamed fixture, a
      # deleted pnpm script or a new handle that no row mentions. The doc-side half runs
      # in docs.yml, which this workflow skips for Markdown-only changes. This is also
      # the run that catches a moved file docs/{en,ja} still names: the move arrives as
      # a code change, so docs.yml never sees it. Before the build so it reports in
      # seconds; it needs no install of its own.
      - run: node scripts/check-assets-index.mjs
      - run: pnpm build
      # Three handles are build-gated. Two are dev-only (import.meta.env.DEV): the meter
      # bench's console handle (src/ui/console.ts) and the keyboard measurement harness
      # (src/ui/keyprobe.ts, installed from src/main.ts). The third, the race harness's
      # plan-ledger probe (src/ui/trace-probe.ts), rides its own build flag (VITE_TRACE,
      # .env.trace) so the instrumented E2E tier can be served a production-SHAPED bundle
      # rather than a dev one. A plain build drops all three statically; this is the guard
      # that they stay dropped. Emitted code only — the source map carries the original
      # text of the dropped branch by design. The other half of each contract — that the
      # handle still carries what its driver uses — is pinned in
      # src/ui/meter-bench.contract.test.ts and src/ui/keyprobe.test.ts.
      - name: Dev-only hooks stay out of the bundle
        # `! grep ...` per line does not work here: POSIX errexit is ignored for a
        # pipeline beginning with `!`, so under the default `bash -e` only the LAST
        # line's status became the step's status and an earlier leak passed silently.
        # An explicit if/exit per handle fails on whichever line actually matches.
        run: |
          if grep -rl --include='*.js' --include='*.html' "__urxConsole" dist; then echo "__urxConsole leaked into the bundle"; exit 1; fi
          if grep -rl --include='*.js' --include='*.html' "__urxKeyProbe" dist; then echo "__urxKeyProbe leaked into the bundle"; exit 1; fi
          if grep -rl --include='*.js' --include='*.html' "__urxTrace" dist; then echo "__urxTrace leaked into the bundle"; exit 1; fi
      - run: pnpm typecheck:e2e
      - run: pnpm test

  # The ordinary E2E suite (--project=chromium): 383 cases, none over 3 s, 3.5 minutes of
  # machine time — about two on the runner's two workers. At that price the alternative,
  # a net that only fires once the change is already on main, costs more than it saves.
  # The race harness is deliberately NOT here; see race.yml for what it costs and why.
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
        timeout-minutes: 10 # hang detector, not a budget — see .github/actions/install-browsers
      # Bounded rather than bare — the action's header has the measurement and the
      # reason. This tier's exposure is the smaller one (29 packages against webkit's
      # 201), but it runs on every pull request, so it is the one seen most often.
      - uses: ./.github/actions/install-browsers
        with:
          browser: chromium
      - run: pnpm test:e2e:app
      # The "list" reporter writes no playwright-report/; traces (trace: on-first-retry,
      # with CI retries) and failure attachments land in the default test-results/, so
      # that is what a failing run has to upload.
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: failure()
        with:
          name: playwright-traces
          path: test-results/
          retention-days: 7
