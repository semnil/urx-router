# コントリビューションガイド

> For the English version, see [CONTRIBUTING.md](CONTRIBUTING.md).

URX Router は単独メンテナーのプロジェクトだが、外部からの協力を歓迎する。特に実機が関わるもの —
本ツールの根拠の大半は公開資料ではなく実測にあるため。

セキュリティ上の問題は issue ではなく [SECURITY.ja.md](SECURITY.ja.md) の手順で報告すること。

## 協力できること

- **手持ちの実機での挙動検証。** URX22 のメーターマッピングはまさにこの形で取り込まれた
  ([#173](https://github.com/semnil/urx-router/pull/173))。パラメータの大半は URX44V で確認したもの
  のため、URX22 / URX44 からの報告は特に有用。
- **issue の登録。** バグ報告・機能要望のテンプレートを使うこと。
- **プルリクエスト。** ルーティング規則やデバイス制御に触れる変更は、先に issue を立てること。
  これらは実機での裏付けが必要で、コードを書く前に根拠を合意しておくほうが早い。

## セットアップ

Node >= 21.2（`?plan=` のコーデックが `CompressionStream("deflate-raw")` を必要とする）、pnpm、
およびデスクトップシェルを触る場合のみ rustup 経由の Rust。

```sh
pnpm install
pnpm dev           # ブラウザ版 http://localhost:5173（Rust 不要）
pnpm tauri dev     # デスクトップアプリ
```

| コマンド | 内容 |
| --- | --- |
| `pnpm build` | `tsc --noEmit` + Vite ビルド |
| `pnpm test` | ユニットテスト（vitest） |
| `pnpm test:e2e` | ポート 4173 の本番ビルドに対する Playwright E2E |
| `pnpm typecheck:e2e` | `e2e/` とルートの設定ファイルの型チェック |
| `pnpm format` | TypeScript ソースへの Prettier 適用 |
| `pnpm check:md` | Markdown テーブルの整合性チェック |
| `pnpm check:skips` | `e2e/race` の恒久 skip が、その理由を保つものと結び付けて登録されているか |
| `pnpm build:demo` | ブラウザデモ版のビルド（保存ダイアログと画像出力を除く） |

## 規約

- 識別子とコメントは英語。コメントは振る舞いのみを説明する（経緯は書かない）。
- diff は最小限に、周囲のスタイルに合わせる。フォーマットのみの変更は単独では受け付けない。
- **ランタイムの外部依存はゼロ** — 配布物に npm パッケージや CDN を持ち込まない。開発依存は可。
- コミット前に `pnpm format`（Rust の変更があれば `cargo fmt`）を実行する。`format.yml` は両方を
  実行して差分が出たら失敗する。ブランチへの書き戻しは行わない。
- `src/style.css` のテーマパレットと `src/ui/graph.ts` の `PALETTES` は同期を保つ。
- **`src/i18n/en.ts` の文字列はすべて `dev()` / `fixed()` / `tr()` のいずれかで包んであり、キーを
  追加するときはどれかを選ぶ。** `dev()` は本体の画面にあるコントロールを写した行で、URX は 3 つの
  表示言語のどれを選んでも当該画面が英語のままなので、どのカタログも同じ文字列を繰り返す（`ja.ts` に
  訳語を書くとコンパイルが通らない）。`fixed()` は実機以外の理由で全言語同一にするもので、対象は
  CONSOLE のストリップ区切りラベルだけ（縦書きのため全角字がラックの幾何を動かす）。`tr()` はアプリ
  自身の文言 — 見出し・凡例・ヒント・ステータス・エラー文 — で、各言語が独自の表記を持つ。包み忘れは
  ファイル名を示すコンパイルエラーになる。どれを選ぶかは型では判定できないので、選択理由をプルリクエスト
  に書くこと。実機で確認済みの範囲は `docs/ja/architecture.md`（「多言語対応」→ 用語の統一）にある。

## テスト

`pnpm test` はコア・デバイス制御層・モデル・UI・アプリのエントリを対象とする。エントリのデスクトップ
側は Tauri シェルのスタブに対して走る — `isTauri()` がデバイス層全体を塞いでおり、それ以外の方法では
到達できないため。

各スイートが走る host / stub は共有されている。ファイル名は `*.test-util.ts` で (vitest はテスト
ファイル自体しか収集しないため、テストを探しても見つからない)、CLAUDE.md の "Reusable assets" が
全件を索引している。新しく書く前に既存のものを拡張すること。

**機能追加と UI 変更には `e2e/*.spec.ts` への E2E 追加が必要。** 義務はテストを**書く**ことであり、
ローカルで走らせることではない。通常の E2E ティアはそのプルリクエスト自身の CI が実行し、失敗は
プルリクエスト上に報告される — 読まれるのはそこなので、push 前に何を流すかは各自の判断でよい。

E2E はポート 4173 の本番ビルドに対して走り、ローカルでは既存サーバーを再利用する。`pnpm preview` を
起動したままにしていると別のバンドルを黙ってテストしてしまうため、先に停止すること。

`*.audit.test.ts` は堅牢性監査で見つかった挙動を固定している。意図的に挙動を変える場合は、
削除や放置ではなく対応するテストを書き換えること。

## ドキュメント

`docs/en/` と `docs/ja/` は同期を保つ。図は Mermaid 記法で書く。

Markdown テーブルの行間に注記を挟むとテーブルはそこで終わり、以降の行はすべてその段落に吸収されて
描画されなくなる。注記は適切な位置に置き、テーブルを再開する場合はヘッダーと区切り行を書き直すこと。
`pnpm check:md` がこれを検出する。

## ルーティング規則

一次情報は公式ブロックダイアグラム。ルートテーブルを変更する場合は `src/models/` と
`docs/{en,ja}/device-model.md` を同時に更新し、Claude スキルに同梱するデータを再生成する:

```sh
UPDATE_SKILL=1 pnpm test skill-export
```

生成物は CI で差分検査されるため、更新漏れはビルド失敗になる。

## デバイス制御

`src/core/control/` は実機に書き込む。ここには 3 つの厳格な規則がある:

- **確認済みパラメータのみ。** アドレスとエンコードはすべて接続した実機で検証すること。
  推測したアドレスを `params.ts` に追加しない。
- **失敗した操作は全体を中断する。** デバイスリンクに部分的成功は存在しない。読めなかった
  パラメータは書き込まない、部分的な読み出しでライブ同期を開始しない、送信は最初の失敗で止める。
- **何で検証したかを書く。** プルリクエストに機種と System ファームウェアのバージョンを明記する。

## コミットとプルリクエスト

- Conventional Commits、**件名も本文も英語**、意味単位で分割する。
- **プルリクエストのタイトルと本文も英語**。
- UI 変更には変更前後のスクリーンショットを添える。
- プルリクエストの CI はビルド・`pnpm typecheck:e2e`・`pnpm check:skips`・ユニットテスト・
  通常の E2E スイート・`cargo test` を実行する — この作業は原則として Markdown / docs のみの変更では省略される。例外は
  スキルの生成物であるモデル参照 (`.claude/skills/urx-routing-planner/references/model-*.md`) で、
  Markdown だが生成器との差分を検査するため通常 CI も走る。サードパーティライセンスの生成は
  マージ後に走る。ライブ同期のレース診断ハーネスは、差分がドキュメントだけではない
  プルリクエストと、バージョンを変更するプルリクエストで走る — 約 7 分、他のチェックと並列。
- 文書と設定の検査は上記に関わらず**すべての**プルリクエストで走る — Markdown テーブルの整合性・
  再利用アセット索引・`pnpm check:gates`。
- マージは `ci-required` / `docs-required` / `format` / `race-required` の 4 つを待つ。いずれもすべての
  プルリクエストで結果を返すため、対象外としてスキップされた変更でも「永久に来ないチェック」
  ではなく緑のチェックが付く。これを保つのが `pnpm check:gates` で、`.github/workflows/`
  配下を編集したら実行する。

コントリビュートすることで、その成果物が [MIT ライセンス](LICENSE)の下で提供されることに同意したものとする。
